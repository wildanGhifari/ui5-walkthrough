"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.uuidv4 = exports.generateId = exports._getDateTimeOffset = exports.ExecutionError = void 0;
/**
 *
 */
class ExecutionError extends Error {
    constructor(message, statusCode, messageData, isSAPMessage, headers = {}) {
        super(message);
        this.isCustomError = true;
        this.statusCode = statusCode;
        this.messageData = messageData;
        this.isSAPMessage = isSAPMessage;
        this.headers = headers;
    }
}
exports.ExecutionError = ExecutionError;
function _getDateTimeOffset(isV4) {
    const date = new Date();
    return isV4 ? date.toISOString() : '/Date(' + date.getTime() + '+0000)/';
}
exports._getDateTimeOffset = _getDateTimeOffset;
const CHARS = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789';
/**
 * Generate an ID of a given length.
 *
 * @param length
 * @returns the generated ID
 */
function generateId(length) {
    let result = '';
    while (length--) {
        result += CHARS.charAt(Math.floor(Math.random() * CHARS.length));
    }
    return result;
}
exports.generateId = generateId;
function uuidv4() {
    return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function (c) {
        const r = (Math.random() * 16) | 0, v = c == 'x' ? r : (r & 0x3) | 0x8;
        return v.toString(16);
    });
}
exports.uuidv4 = uuidv4;
//# sourceMappingURL=common.js.map